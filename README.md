# Multiblessed-terminal
Multiplexer terminal application Wrtiien in Nodejs using Blessed and blessed-cntrib. 
To use:

install dependencies:

npm install path 
npm install fs
npm install blessed
npm install blessed-contrib

run:
nodejs multiterm.js
